{{
    {
        title: page.title,
        location: page.header.location,
        time: page.header.time,
        id: page.slug,
        content: page.content,
        media: page.media | map(m => { url: m.url, meta: m.meta }) | array,
    }
    | json_encode | raw
}}
