{{
    {
        title: page.title,
        location: page.location,
        time: page.time,
        id: page.slug,
        content: page.content,
        media: page.media | map(m => { url: m.url, meta: m.meta }) | array,
    }
    | json_encode | raw
}}
