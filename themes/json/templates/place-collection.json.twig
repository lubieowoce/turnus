{{
    {
        meta: page.header,
        count: count(page.collection),
        items: page.collection | map(p => {
            name: p.title,
            id: p.slug,
            description: p.summary(50)|raw
        }),
    }
    | json_encode | raw
}}
